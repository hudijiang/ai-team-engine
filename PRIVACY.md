# 隐私与数据处理说明（本地 PoC）

> 本项目当前定位为**本地单用户 / BYOK 技术预览**，不是企业级 SaaS。

## 数据存储位置

| 数据 | 位置 | 说明 |
|------|------|------|
| API Key | 浏览器 IndexedDB（完整）/ localStorage（去敏 bootstrap） | 不会自动上传到本仓库作者服务器 |
| 会话、消息、检查点 | 浏览器 IndexedDB | 与域名绑定，清站点数据即删除 |
| Prompt 调试日志 | 浏览器内存 + 持久化片段 | 已做基础脱敏，仍可能含业务文本 |
| 开发环境文件日志 | 本机 `logs/`（仅 `npm run dev`） | 限体积与条数，含基础脱敏 |

## 会离开浏览器的数据

- 调用模型时，**浏览器**把请求发到你填写的 URL（含 Prompt、部分上下文、鉴权头中的 API Key）。
- 风险是：密钥与业务文本外泄、恶意反向代理、以及该 URL 指向本机/内网服务。当前**没有**服务端代发，因此不构成服务端 SSRF。
- 可选的「MCP」配置走的是简化 HTTP（`/tools/list`、`/tools/call`），不是官方 MCP；默认不会执行高风险调用。
- 配置页不得再声称「不会上传到任何服务器」。

## 敏感信息

系统会尝试对验证码、Token、常见密钥模式做脱敏，并：

- 高风险 HITL：不把凭据原文注入 LLM Prompt / 对话气泡
- 工具参数与审计预览脱敏
- 导出报告时对已知敏感模式脱敏

**不能保证 100% 覆盖所有语言与格式**。请勿在 PoC 中粘贴真实生产密钥、客户 PII 或支付凭据。

## 数据保留

- 默认无限期保留在本地，直到用户清除站点数据或使用应用内重置。
- 无服务端删除/导出合规流程（P1+ 后端版本再提供）。

## 第三方

模型供应商、以及你自行填写的 HTTP 工具桥的隐私政策由其运营方负责。使用前请自行审阅。

## 范围

本文针对**本地单用户 PoC**。服务端删除/导出、密钥托管审计等，要等单租户 Gateway 落地后再写。多租户不在当前范围。
