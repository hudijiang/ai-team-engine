# 隐私与数据处理说明（本地 PoC）

> 本项目定位为**本地单用户 / 单租户预览**。不规划对外 SaaS 或多租户。

## 数据存储位置

| 数据 | 位置 | 说明 |
|------|------|------|
| 供应商 API Key | 直连模式：浏览器 IndexedDB；Gateway 模式：Gateway 进程环境变量 | localStorage bootstrap 不保存完整密钥 |
| Gateway Token | 浏览器 IndexedDB | 仅发往用户配置的 Gateway；仍应视为敏感凭据 |
| 会话、消息、检查点 | 浏览器 IndexedDB | 与域名绑定，清站点数据即删除 |
| Gateway 运行记录 | Gateway 本机磁盘 | 仅为对账记录；关闭标签页后 Agent 不会继续执行 |
| Prompt 调试日志 | 浏览器内存 + 持久化片段 | 已做基础脱敏，仍可能含业务文本 |
| 开发环境文件日志 | 本机 `logs/`（仅 `npm run dev`） | 限体积与条数，含基础脱敏 |

## 会离开浏览器的数据

- **直连模式**：浏览器把 Prompt、部分上下文和供应商 API Key 发往用户填写的 URL，存在恶意代理及访问本机/内网地址的风险。
- **Gateway 模式**：浏览器把 Prompt、模型 ID 和 Gateway Token 发给本机 Gateway；供应商 Key 不进入浏览器。Gateway 会服务端代发，因此按服务端 SSRF 边界使用固定 Provider、主机白名单和私网拒绝。
- 可选的「MCP」配置走的是简化 HTTP（`/tools/list`、`/tools/call`），不是官方 MCP；默认不会执行高风险调用。
- 配置页不得再声称「不会上传到任何服务器」。

## 敏感信息

系统会尝试对验证码、Token、常见密钥模式做脱敏，并：

- 高风险 HITL：不把凭据原文注入 LLM Prompt / 对话气泡
- 工具参数与审计预览脱敏
- 导出报告时对已知敏感模式脱敏

**不能保证 100% 覆盖所有语言与格式**。请勿在 PoC 中粘贴真实生产密钥、客户 PII 或支付凭据。

## 数据保留

- 默认无限期保留在本地，直到用户清除站点数据或使用应用内重置。
- 无完整的服务端删除/导出合规流程；Gateway 运行记录也尚无自动保留期限。

## 第三方

模型供应商、以及你自行填写的 HTTP 工具桥的隐私政策由其运营方负责。使用前请自行审阅。

## 范围

本文针对**本地单用户 + 可选本机 Gateway**。Gateway 可托管供应商密钥。服务端任务执行、完整删除/导出不在当前范围；多租户不在规划内。
